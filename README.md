# Deep_Unfolding
This repository implements a deep unfolding (deep unrolling) framework, where iterative optimization algorithms are transformed into structured neural network layers.Each layer mimics one iteration of the original algorithm, enabling faster convergence and improved performance through learning.
